
tokenize, parse and evaluate nested lisp expressions
composed of integers/floating point numbers and standard
arithmetic operators.

Expressions can contain variables: example: (+ a (* 2 b))

Implement "if" expressions. 

Conditional operators added: <, >, = 

